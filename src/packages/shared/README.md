# Shared Contracts (TypeScript)

Shared TypeScript types for the Caddie repo. Import these to keep frontend and backend contracts aligned.

## Files and purpose
- `README.md` - Overview of the shared TypeScript package.
- `index.ts` - Exported TypeScript types for hubs, sources, and chat.
- `package.json` - Package metadata for the shared module.
