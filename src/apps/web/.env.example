NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Leave blank for localhost/dev; the app will use the current browser origin.
# Set this in deployed environments to the real site base URL, for example https://your-site.example
NEXT_PUBLIC_AUTH_REDIRECT_BASE_URL=
