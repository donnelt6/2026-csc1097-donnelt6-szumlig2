// page.tsx: Auth page server wrapper that renders the login client component.

import { AuthPageClient } from "../../components/auth/AuthPageClient";

export default function AuthPage() {
  return <AuthPageClient />;
}
