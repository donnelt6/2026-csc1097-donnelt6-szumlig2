[build]
  base = "src"
  command = "npm install && npm --workspace apps/web run build"
  publish = "apps/web/.next"

[build.environment]
  NODE_VERSION = "22"

[[plugins]]
  package = "@netlify/plugin-nextjs"
